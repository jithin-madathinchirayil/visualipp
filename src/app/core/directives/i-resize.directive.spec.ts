import { IResizeDirective } from './i-resize.directive';

describe('IResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new IResizeDirective();
    expect(directive).toBeTruthy();
  });
});
